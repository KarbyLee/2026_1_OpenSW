<h1 align="center">
  나만의 캘린더<br/>
  -Design document-<br/><br/>
  <img src="Images/calenderImg.png", height="300x", width="300px"><br/>  
</h1>
<body style="font-size: 20px;">22412073, 이아림, forleer179@gmail.comm</body>

<h2 align="center">[ Revision history ]</h2>
<table>
  <tr>
    <th>Revision date</th>
    <th>Version #</th>
    <th>Description</th>
    <th>Author</th>
  </tr>
  <tr>
    <th>2026-05-26</th>
    <th>#1.0.0</th>
    <th>First Design Documentation</th>
    <th></th>
  </tr>
  <tr>
    <th></th>
    <th></th>
    <th></th>
    <th></th>
  </tr>
</table>

<h2 align="center">= Contents =</h2>
<h3>
  1. Introduction<br/>
  1.1 Summary<br/>
  1.2 Important Points of Design<br/>
  2. Class Diagram<br/>
  2.1 Overall Class Diagram<br/>
  2.2 Class Diagram Description<br/>
  3. Sequence Diagram<br/>
  3.1 View Calendar<br/>
  3.2 Change Month<br/>
  3.3 Manage Memo<br/>
  3.4 Open Deco Page<br/>
  3.5 Save Calendar Design<br/>
  4. State machine diagram<br/>
  5. Implementation requirements<br/>
  6. Glossary<br/>
  7. References<br/>
</h3>

<h2>1. Introduction</h2>
본 문서는 Analysis 단계의 문서에 이어지는 Design 단계의 문서다. 이 문서는 C:\2026_1\pretyCalendar 경로에 있는 실제 구현 파일을 기준으로 나만의 캘린더 시스템의 구조와 동작 방식을 설계 관점에서 설명한다. 본 프로젝트는 HTML, CSS, JavaScript를 이용한 웹 기반 캘린더이며, 캘린더 조회, 공휴일 표시, 날짜별 메모 저장, 사이드 메뉴, 캘린더 꾸미기 기능으로 구성된다.

<h3>1.1 Summary</h3>
나만의 캘린더는 사용자가 월별 캘린더를 확인하고 특정 날짜에 메모를 작성할 수 있는 프로그램이다. 기본 캘린더 화면은 index.html, renderer.js, style.css로 구성되며, 사용자는 이전 달과 다음 달로 이동할 수 있고 날짜 칸을 클릭하여 메모 모달을 열 수 있다. 메모는 브라우저 localStorage에 저장되므로 별도의 서버 없이도 사용자의 입력 데이터를 유지할 수 있다.<br/>
또한 이 시스템은 공공데이터포털 공휴일 API를 호출하여 해당 연도의 공휴일 정보를 가져온다. 가져온 공휴일은 날짜 칸에 표시되며, 한 번 가져온 연도별 공휴일 데이터는 holidayCache에 저장되어 같은 연도를 다시 볼 때 중복 호출을 줄인다.<br/>
캘린더 꾸미기 화면은 decorate.html, decorate.js, decorate.css로 구성된다. 사용자는 미리보기 캘린더를 보면서 폰트 종류, 폰트 크기, 요일별 글자 색상, 배경 색상, 날짜 칸 색상, 투명도, 하이라이트 색상을 조정할 수 있다. 꾸미기 설정 역시 localStorage에 저장되어 이후 다시 불러올 수 있다.

<h3>1.2 Important Points of Design</h3>
- 사용자 경험 기반의 간단한 캘린더 조작<br/>
- 날짜별 메모를 빠르게 작성, 수정, 삭제할 수 있는 모달 구조<br/>
- 공휴일 API와 캐시를 이용한 공휴일 표시<br/>
- 서버 없이 localStorage를 이용하는 가벼운 데이터 저장 방식<br/>
- 캘린더 화면과 꾸미기 화면을 분리한 페이지 구조<br/>
- 꾸미기 변경 결과를 즉시 확인할 수 있는 미리보기 중심 설계<br/>

<h2>2. Class Diagram</h2>
본 프로젝트는 JavaScript 기반으로 구현되어 명시적인 class 문법을 많이 사용하지는 않는다. 따라서 본 설계 문서에서는 실제 파일 안의 DOM 요소, 함수, 저장 데이터, 모듈 역할을 기준으로 클래스를 개념화하여 표현한다. View는 HTML과 CSS가 담당하고, 주요 동작 로직은 renderer.js와 decorate.js가 담당한다. 이미지는 생략한다.

<h3>2.1 Overall Class Diagram</h3>
전체 구조는 크게 CalendarView, CalendarController, MemoStore, HolidayService, SidebarController, DecoView, DecoController, DecoStore로 나눌 수 있다. CalendarView는 기본 캘린더 화면을 표시하고 CalendarController는 날짜 계산과 렌더링을 수행한다. MemoStore는 localStorage의 calendarMemos 값을 관리하며, HolidayService는 공휴일 API 호출과 캐시를 담당한다. SidebarController는 옵션 아이콘과 사이드 메뉴의 열림/닫힘을 관리한다. DecoView와 DecoController는 꾸미기 페이지의 미리보기와 사용자 입력 처리를 담당하고, DecoStore는 꾸미기 설정을 localStorage에 저장한다.

<h3>2.2 Class Diagram Description</h3>
<strong>CalendarView</strong>
<table>
  <tr>
    <th>Class Description</th>
    <td>index.html의 캘린더 화면을 구성하는 View 역할이다. 월 제목, 요일 헤더, 날짜 보드, 메모 모달, 사이드바를 화면에 표시한다.</td>
  </tr>
  <tr>
    <th>Attributes</th>
    <td>dateTitle, dateBoard, modalOverlay, memoInput, sidebar, sidebarOverlay, optionIcon</td>
  </tr>
  <tr>
    <th>Operations</th>
    <td>날짜 칸 클릭 이벤트 연결, 모달 표시, 사이드바 표시, 월 이동 버튼 표시</td>
  </tr>
</table>

<strong>CalendarController</strong>
<table>
  <tr>
    <th>Class Description</th>
    <td>renderer.js에서 캘린더 날짜 계산과 화면 렌더링을 담당하는 Controller 역할이다.</td>
  </tr>
  <tr>
    <th>Attributes</th>
    <td>date, selectedDateKey</td>
  </tr>
  <tr>
    <th>Operations</th>
    <td>makeDateKey, makeCalendar, openModal, closeModal, prevDay onclick, nextDay onclick</td>
  </tr>
</table>

<strong>HolidayService</strong>
<table>
  <tr>
    <th>Class Description</th>
    <td>공공데이터포털 공휴일 API를 호출하고 연도별 공휴일 데이터를 캐싱하는 Service 역할이다.</td>
  </tr>
  <tr>
    <th>Attributes</th>
    <td>SERVICE_KEY, API_URL, holidayCache</td>
  </tr>
  <tr>
    <th>Operations</th>
    <td>fetchHolidaysByYear</td>
  </tr>
</table>

<strong>MemoStore</strong>
<table>
  <tr>
    <th>Class Description</th>
    <td>날짜별 메모 데이터를 localStorage에 저장하고 불러오는 저장소 역할이다.</td>
  </tr>
  <tr>
    <th>Attributes</th>
    <td>calendarMemos</td>
  </tr>
  <tr>
    <th>Operations</th>
    <td>getMemos, saveMemos, btnSave onclick, btnDelete onclick</td>
  </tr>
</table>

<strong>SidebarController</strong>
<table>
  <tr>
    <th>Class Description</th>
    <td>옵션 아이콘을 통해 사이드바를 열고 닫으며, 캘린더 꾸미기 페이지로 이동하는 기능을 담당한다.</td>
  </tr>
  <tr>
    <th>Attributes</th>
    <td>sidebar, sidebarOverlay, optionIcon</td>
  </tr>
  <tr>
    <th>Operations</th>
    <td>openSidebar, closeSidebar, optionIcon click event, sidebarOverlay click event</td>
  </tr>
</table>

<strong>DecoView</strong>
<table>
  <tr>
    <th>Class Description</th>
    <td>decorate.html의 캘린더 꾸미기 화면을 구성하는 View 역할이다. 왼쪽 미리보기 캘린더와 오른쪽 꾸미기 사이드바를 포함한다.</td>
  </tr>
  <tr>
    <th>Attributes</th>
    <td>previewCal, previewBoard, fontFamily, fontSize, color picker panels, btnDecoSave, btnDecoApply, btnDecoExit</td>
  </tr>
  <tr>
    <th>Operations</th>
    <td>미리보기 캘린더 표시, 폰트 입력 표시, 색상 선택 패널 표시, 저장/적용/나가기 버튼 표시</td>
  </tr>
</table>

<strong>DecoController</strong>
<table>
  <tr>
    <th>Class Description</th>
    <td>decorate.js에서 꾸미기 입력값을 처리하고 미리보기 캘린더에 즉시 반영하는 Controller 역할이다.</td>
  </tr>
  <tr>
    <th>Attributes</th>
    <td>LIGHT_THEME, DARK_THEME, fontColors, currentBg, defaultCellColor, otherCellColor, highlightColor, opacity values</td>
  </tr>
  <tr>
    <th>Operations</th>
    <td>renderPreview, makePicker, applyFontColors, applyBg, applyDefaultCellColor, applyOtherColor, applyHighlightColor, applyTheme, applyFontSize</td>
  </tr>
</table>

<strong>DecoStore</strong>
<table>
  <tr>
    <th>Class Description</th>
    <td>사용자가 저장한 꾸미기 설정을 localStorage에 저장하고 다시 불러오는 저장소 역할이다.</td>
  </tr>
  <tr>
    <th>Attributes</th>
    <td>decoFont, decoFontSize, decoBg, decoFontColor-week, decoFontColor-sat, decoFontColor-sun, decoDefaultCellColor, decoOtherColor, decoOtherOpacity, decoHighlightColor, decoHighlightOpacity</td>
  </tr>
  <tr>
    <th>Operations</th>
    <td>btnDecoSave click event, DOMContentLoaded saved setting load</td>
  </tr>
</table>

<h2>3. Sequence Diagram</h2>
이 장에서는 주요 Use Case의 동작 순서를 설명한다. 실제 다이어그램 이미지는 생략하고, 객체 간 호출 순서를 텍스트로 정리한다.

<h3>3.1 View Calendar</h3>
View Calendar는 사용자가 index.html을 실행하면서 시작된다. 브라우저가 문서를 로드하면 window.onload 이벤트가 실행되고 CalendarController의 makeCalendar(date)가 호출된다. makeCalendar는 현재 연도와 월을 계산한 뒤 HolidayService의 fetchHolidaysByYear(currentYear)를 호출한다. HolidayService는 공휴일 데이터가 holidayCache에 있으면 캐시 값을 반환하고, 없으면 공공데이터포털 API를 호출한다. 이후 CalendarController는 MemoStore의 getMemos()를 통해 저장된 메모를 읽고, 날짜 칸 HTML을 생성하여 CalendarView의 dateBoard에 삽입한다.

<h3>3.2 Change Month</h3>
Change Month는 사용자가 이전 달 또는 다음 달 버튼을 클릭하면서 시작된다. 이전 달 버튼을 누르면 date.setMonth(date.getMonth() - 1)이 실행되고, 다음 달 버튼을 누르면 date.setMonth(date.getMonth() + 1)이 실행된다. 이후 makeCalendar(new Date(date))가 호출되어 변경된 월의 날짜, 공휴일, 메모가 다시 계산된다. CalendarView는 새로 생성된 날짜 보드를 화면에 표시한다.

<h3>3.3 Manage Memo</h3>
Manage Memo는 사용자가 캘린더의 특정 날짜 칸을 클릭하면서 시작된다. 날짜 칸에는 data-key, data-year, data-month, data-day, data-holiday 값이 저장되어 있다. 사용자가 날짜 칸을 클릭하면 openModal이 호출되고, MemoStore의 getMemos()로 기존 메모를 읽어 memoInput에 표시한다. 사용자가 저장 버튼을 누르면 btnSave 이벤트가 실행되어 입력값을 calendarMemos에 저장하고, 삭제 버튼을 누르면 btnDelete 이벤트가 실행되어 해당 날짜의 메모를 제거한다. 저장 또는 삭제 이후 closeModal이 호출되고 makeCalendar(date)가 다시 실행되어 변경된 메모 미리보기가 캘린더에 반영된다.

<h3>3.4 Open Deco Page</h3>
Open Deco Page는 사용자가 왼쪽 상단 옵션 아이콘을 클릭하면서 시작된다. optionIcon click event는 사이드바가 열려 있는지 확인한 뒤 openSidebar 또는 closeSidebar를 실행한다. openSidebar는 sidebar에 open 클래스를 추가하고 sidebarOverlay에 active 클래스를 추가한다. 사용자가 사이드바 안의 캘린더 꾸미기 항목을 클릭하면 location.href='decorate.html'이 실행되어 꾸미기 화면으로 이동한다.

<h3>3.5 Save Calendar Design</h3>
Save Calendar Design은 사용자가 decorate.html에서 꾸미기 값을 변경하고 저장 버튼을 누르면서 시작된다. DecoController는 fontFamily, fontSize, 색상 피커, 투명도 슬라이더의 입력 이벤트를 받아 previewCal에 즉시 반영한다. 사용자가 저장 버튼을 누르면 btnDecoSave 이벤트가 실행되고 DecoStore는 폰트, 폰트 크기, 배경 색상, 글자 색상, 날짜 칸 색상, 투명도 값을 localStorage에 저장한다. 이후 사용자가 다시 꾸미기 화면을 열면 DOMContentLoaded 단계에서 저장된 값들을 읽어 미리보기 캘린더에 반영한다.

<h2>4. State machine diagram</h2>
본 프로그램의 상태는 크게 Calendar Page, Memo Modal, Sidebar Open, Deco Page, Saved Design 상태로 구분할 수 있다. 사용자가 프로그램을 실행하면 Calendar Page 상태가 된다. Calendar Page 상태에서는 월 이동 버튼을 눌러 다시 Calendar Page 상태로 갱신될 수 있고, 날짜 칸을 클릭하면 Memo Modal 상태로 이동한다. Memo Modal 상태에서 저장, 삭제, 닫기 동작이 끝나면 다시 Calendar Page 상태로 돌아간다. Calendar Page 상태에서 옵션 아이콘을 누르면 Sidebar Open 상태가 되고, 오버레이를 클릭하거나 아이콘을 다시 누르면 Calendar Page 상태로 돌아간다. Sidebar Open 상태에서 캘린더 꾸미기 메뉴를 클릭하면 Deco Page 상태로 이동한다. Deco Page 상태에서 사용자는 꾸미기 값을 변경할 수 있으며 저장 버튼을 누르면 Saved Design 상태가 된다. 나가기 버튼을 누르면 다시 Calendar Page 상태로 이동한다.

<h2>5. Implementation requirements</h2>
<strong>H/W platform requirements</strong>
<table>
  <tr>
    <th>Processor</th>
    <td>Intel or AMD dual-core or higher CPU</td>
  </tr>
  <tr>
    <th>Memory</th>
    <td>4GB RAM or higher</td>
  </tr>
  <tr>
    <th>Storage</th>
    <td>100MB usable space or higher</td>
  </tr>
</table>

<strong>S/W platform requirements</strong>
<table>
  <tr>
    <th>OS</th>
    <td>Windows 10 or higher</td>
  </tr>
  <tr>
    <th>Browser</th>
    <td>Chrome, Edge, or modern browser supporting HTML5, CSS3, JavaScript, localStorage, fetch API</td>
  </tr>
  <tr>
    <th>Network</th>
    <td>Internet connection is required for holiday API data. Calendar and memo functions can work without API response.</td>
  </tr>
</table>

<strong>Implementation constraints</strong>
<table>
  <tr>
    <th>Storage</th>
    <td>메모와 꾸미기 설정은 서버가 아닌 브라우저 localStorage에 저장한다.</td>
  </tr>
  <tr>
    <th>API</th>
    <td>공휴일 정보는 공공데이터포털 API를 사용하며, API 오류 시 공휴일 없이 캘린더를 표시한다.</td>
  </tr>
  <tr>
    <th>UI</th>
    <td>기본 캘린더 화면과 꾸미기 화면을 별도 HTML 파일로 분리한다.</td>
  </tr>
</table>

<h2>6. Glossary</h2>
<table>
  <tr>
    <th>Term</th>
    <th>Description</th>
  </tr>
  <tr>
    <th>CalendarView</th>
    <td>기본 캘린더 화면을 표시하는 View 역할</td>
  </tr>
  <tr>
    <th>CalendarController</th>
    <td>캘린더 날짜 계산, 렌더링, 월 이동을 처리하는 Controller 역할</td>
  </tr>
  <tr>
    <th>HolidayService</th>
    <td>공휴일 API 호출과 연도별 공휴일 캐시를 담당하는 Service 역할</td>
  </tr>
  <tr>
    <th>MemoStore</th>
    <td>날짜별 메모를 localStorage에 저장하고 불러오는 저장소 역할</td>
  </tr>
  <tr>
    <th>DecoController</th>
    <td>캘린더 꾸미기 입력값을 미리보기 화면에 적용하는 Controller 역할</td>
  </tr>
  <tr>
    <th>DecoStore</th>
    <td>캘린더 꾸미기 설정을 localStorage에 저장하고 불러오는 저장소 역할</td>
  </tr>
  <tr>
    <th>localStorage</th>
    <td>브라우저 내부에 key-value 형태로 데이터를 저장하는 웹 저장소</td>
  </tr>
</table>

<h2>7. References</h2>
1) C:\2026_1\pretyCalendar\index.html<br/>
2) C:\2026_1\pretyCalendar\renderer.js<br/>
3) C:\2026_1\pretyCalendar\style.css<br/>
4) C:\2026_1\pretyCalendar\decorate.html<br/>
5) C:\2026_1\pretyCalendar\decorate.js<br/>
6) C:\2026_1\pretyCalendar\decorate.css<br/>
7) C:\Users\dldkf\Downloads\3. Design with examples\3. [Design] Example 8.pdf<br/>
